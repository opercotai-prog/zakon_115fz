cat > requirements.txt <<'EOF'
# Для Streamlit (основной фреймворк)
streamlit>=1.28.0

# Для Flask (если понадобится локально)
Flask==2.3.3
flask-cors==4.0.0
Werkzeug==2.3.7

# Общие зависимости
requests==2.31.0
python-docx==1.1.0
pdfplumber==0.10.3
EOF
